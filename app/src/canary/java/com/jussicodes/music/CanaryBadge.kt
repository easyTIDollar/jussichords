package com.jussicodes.music

/**
 * Canary-only code: this class is compiled into the canary APK only.
 * The debug and release variants do NOT contain it, so any bold
 * experiments go here (app/src/canary/) and stay isolated.
 */
object CanaryBadge {
    const val CHANNEL = "canary"
}
